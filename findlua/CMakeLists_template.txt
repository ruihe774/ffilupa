project(LuaFinder C)
cmake_minimum_required(VERSION 3.8)
find_package(Lua %s EXACT REQUIRED)
message(FOUND)
message("${LUA_LIBRARIES}")
message("${LUA_INCLUDE_DIR}")
message("${LUA_VERSION_STRING}")
message("${LUA_VERSION_MAJOR}")
message("${LUA_VERSION_MINOR}")
message("${LUA_VERSION_PATCH}")
